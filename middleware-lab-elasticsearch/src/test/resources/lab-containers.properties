# Filtered by Maven from the parent pom (spring-boot-starter-parent uses @...@ delimiters).
# Change the version in the parent pom property `elasticsearch.image`, not here.
elasticsearch.image=@elasticsearch.image@
